#include <stdio.h>

int main(){
    int numero1
    float temp,sqrt

    printf("inserisci un numero:\n")
    scanf("%d", &numero);
    temp = 0
    while(sqrt!=temp){
        temp=sqrt;
        sqrt=(numero/temp+temp)/2;
    }
}
